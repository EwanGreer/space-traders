<% content_for :title, "Factions" %>

<div class="container my-4">
  <% if notice.present? %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= notice %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% end %>

  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3">Factions</h1>
    <%= link_to "New Faction", new_faction_path, class: "btn btn-primary" %>
  </div>

  <% if @factions.any? %>
    <div class="row g-4">
      <% @factions.each do |faction| %>
        <div class="col-sm-6 col-md-4 col-lg-3">
          <div class="card h-100 shadow-sm">
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= faction.name %></h5>
              <p class="card-text text-truncate"><%= faction.symbol %></p>
              <div class="mt-auto">
                <%= link_to "Show", faction, class: "btn btn-outline-primary btn-sm" %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-info" role="alert">
      No factions found. <%= link_to "Create a faction", new_faction_path, class: "alert-link" %>.
    </div>
  <% end %>
</div>
